# Implementation Plan: GitHub Pages Deployment Fix

**Branch**: `001-fix-gh-pages-deploy` | **Date**: 2026-02-26 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/001-fix-gh-pages-deploy/spec.md`

## Summary

Fix static-site deployment so GitHub Pages always publishes from `src/` and includes compiled Tailwind CSS artifacts. The implementation updates deployment workflow steps, validates required build outputs before publish, and documents deterministic deployment verification.

## Related Artifacts

- Specification: `specs/001-fix-gh-pages-deploy/spec.md`
- Task breakdown: `specs/001-fix-gh-pages-deploy/tasks.md`
- Verification runbook: `specs/001-fix-gh-pages-deploy/quickstart.md`

## Technical Context

**Language/Version**: HTML5, CSS3, JavaScript (ES6+)  
**Primary Dependencies**: Tailwind CSS CLI, Prettier, GitHub Actions  
**Storage**: N/A (static files)  
**Testing**: Manual deployment verification + workflow run validation  
**Target Platform**: GitHub Pages static hosting  
**Project Type**: Single-project static web app  
**Performance Goals**: No regression in page load rendering; CSS available at first paint  
**Constraints**: Publish source must be `src/`; build step must run before publish  
**Scale/Scope**: Deployment pipeline and docs updates for one static portfolio site

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- Anime-first design: preserved, no visual redesign introduced.
- Content transparency: unchanged data-driven content flow.
- Mobile-responsive core: unchanged layout behavior.
- Performance obsession: enforced by guaranteed CSS build before publish.
- Accessibility by default: no semantic/accessibility regressions introduced.
- Infrastructure as code mindset: deployment behavior codified in workflow and docs.
- Edutainment: no impact on content style.

Gate result: pass.

## Project Structure

### Documentation (this feature)

```text
specs/001-fix-gh-pages-deploy/
├── plan.md
├── spec.md
└── tasks.md
```

### Source Code (repository root)

```text
.github/
└── workflows/
    └── *.yml                # GitHub Pages build/deploy workflow(s)

src/
├── index.html
├── css/
│   ├── styles.css
│   └── tailwind.css
└── js/
    ├── main.js
    ├── animations.js
    └── data.js

package.json                 # Build scripts (build-css, build)
README.md                    # Local run/build instructions
```

**Structure Decision**: Single static-site project. Changes are limited to workflow configuration, optional deployment guard scripts/config, and deployment documentation.

## Complexity Tracking

No constitution violations expected; section intentionally empty.

## Foundational Deployment Assumptions

- GitHub Pages deployment is executed by `.github/workflows/static.yml`.
- Artifact upload source for publishing remains `src/`.
- CSS output file `src/css/tailwind.css` is generated by `npm run build` before upload.
- Deployment must fail fast if required static artifacts are missing.
